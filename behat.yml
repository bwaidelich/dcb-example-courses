default:
  autoload:
    '': '%paths.base%/tests/Behat'
  suites:
    default:
      paths: ['%paths.base%/tests/Behat']
      parameters:
        env(DCB_TEST_DSN): "foo"
      contexts:
        - Wwwision\DCBExample\Tests\Behat\Bootstrap\FeatureContext:
            eventStoreDsn: '%env(DCB_TEST_DSN)%'
  formatters:
    pretty: false
    progress: true